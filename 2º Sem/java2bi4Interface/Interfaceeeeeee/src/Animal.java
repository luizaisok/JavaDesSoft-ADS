public interface Animal { // nao eh class, nao tah usando o termo
    
    /* Todas as classes que IMPLEMENTS esta interface escreve (implementar) os métodos com "assinaturas" definidas abaixo */
    void som();
    void mover();
}